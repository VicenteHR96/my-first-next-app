import React from "react";

export const metadata = {
  title: "Favoritos - Capellari Tienda Oficial",
  description: "Encuentra los mejores electrodomésticos aquí.",
};

const Layout = ({ children }) => {
  return <>{children}</>;
};

export default Layout;
